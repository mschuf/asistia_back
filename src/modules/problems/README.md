# Problems module (placeholder)

Iteraci├│n siguiente. Mapea al recurso `/Problem` de GLPI 9.4.2 apirest.php.

Pendiente:
- DTOs (`CreateProblemDto`, `ListProblemsQueryDto`).
- `ProblemsService` con flujo ITIL.
- `ProblemsGlpiRepository` en `modules/glpi/repositories/`.
- Endpoints `GET/POST/PATCH /v1/problems`.
