# Changes module (placeholder)

Iteraci├│n siguiente. Mapea al recurso `/Change` de GLPI 9.4.2 apirest.php.

Pendiente:
- DTOs (`CreateChangeDto`).
- `ChangesService` con aprobaci├│n / planificaci├│n.
- `ChangesGlpiRepository` en `modules/glpi/repositories/`.
- Endpoints `GET/POST/PATCH /v1/changes`.
